package CB_Utils.Settings;

/**
 * Global = config.db3<br>
 * Local = aktuelle DB<br>
 * Platform = über den PlatformConnector
 *
 * @author Longri
 */
public enum SettingStoreType {
    Global, Local, Platform
}
